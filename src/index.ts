// open project dev-image in your IDE and open terminal in this directory
// $ docker build -t node-dev-image .
// $ docker run -it --rm --name dev -v $(pwd):/code node-dev-image bash
// $ cd code
// $ yarn
// $ yarn dev

// dat to do docker-compose a v ramci dockerfile zutomatizovat cd code && yarn && yarn dev

console.log('Qest is best')